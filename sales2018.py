import numpy as np
a = np.array([[0, 1, 2, 3, 4], [9, 8, 7, 6]])
print(a.ndim)
print(a.shape)
print(a.size)
print(a.dtype)
print(a.itemsize)

#x = np.array(list / tuple, dtype=np.float32)
x = np.array.()[0, 1, 2, 3]
